TypeDesc("Soldier")
{
	Flag(0x1); //MUST BE UNIQUE OR A FATE WORSE THAN DEATH WILL BEFALL YOU  (trust me)
	Color(180,0,0);
}

TypeDesc("Hover") //duh, anything that hovers, for over water vehicles
{
	Flag(0x2);	
	Color(90,0,90);
}

TypeDesc("Small") //STAP, Speederbike, personal one man carriers
{
	Flag(4);	
	Color(0,180,0);
}

TypeDesc("Medium") //hover, walker, any standard type vehicle
{
	Flag(8);	
	Color(90,90,180);
}

TypeDesc("Huge") //giant walker ATT or other huge vehicle
{
	Flag(16);	
	Color(90,100,0);
}

TypeDesc("Flyer") //Flyer
{
    Flag(32);
    Color(90,0,180);
}
